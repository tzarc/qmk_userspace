#!/bin/bash

if [ "$1" != "$2" ]; then
    [ -e lib/ugfx ] && rm -rf lib/ugfx
    [ -e lib/chibios-contrib/ext/mcux-sdk ] && rm -rf lib/chibios-contrib/ext/mcux-sdk
    git submodule sync --recursive --quiet
    git submodule update --init --recursive --progress --quiet
    ../bin/qmk --update
fi
